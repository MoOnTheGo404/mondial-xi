# Resume bullets (all measured/verified in this repo)

Pick 3; keep the numbers exactly as generated by the artifacts.

1. Built **Kickoff Atlas**, a full-stack football forecasting platform
   (FastAPI + Next.js 16 + Python ML monorepo) on 49,495 public-domain
   international matches (1872–2026); chronologically evaluated five models
   and shipped a calibrated gradient-boosting champion scoring **0.870 log
   loss / 60.8% top-pick accuracy / 0.014 ECE on an untouched 3,689-match
   test window** (vs 1.054 naive baseline).

2. Engineered a leak-proof chronological feature pipeline (emit-before-update
   Elo/form state over 154 years of matches, 0.4 s full pass) with a
   temporal-integrity test suite proving future-result tampering and dataset
   truncation cannot alter historical feature vectors.

3. Designed a vectorized Monte Carlo tournament engine implementing FIFA's
   2026 rules (head-to-head-first tiebreakers, best-thirds constraint
   allocation, ET/penalties) that simulates **10,000 World Cup outcomes in
   ~0.37 s** from the real in-progress bracket, with locked-result what-ifs
   and deterministic seeds.

4. Implemented an auditable forecasting track record: prospective predictions
   stored as immutable SHA-256-hashed snapshots before kickoff and scored
   post-match (RPS/Brier/log loss), with backtests strictly segregated —
   plus 78 unit/integration tests, 12 Playwright journeys, and CI that
   rebuilds all artifacts from source.

5. Conducted licensing-driven data sourcing across 6+ providers, rejecting
   feeds without publication rights, and shipped honest graceful degradation:
   every capability reports provider status, license constraints and
   retrieval timestamps instead of fabricated data.
